const textElement = document.getElementById("message");

// Set a timeout to change the text after 3 seconds (3000 milliseconds)
setTimeout(function () {
  textElement.innerText = "X-cluding the mainstream";
}, 3000);
